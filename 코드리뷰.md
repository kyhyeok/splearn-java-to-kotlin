# 코드 리뷰 보고서

- 작성일: 2026-09-19
- 대상: `main` 브랜치 HEAD(`4962f64`) + 미커밋 작업 트리(Curriculum 애그리거트 WIP 24개 파일)
- 기준: **실서비스 배포 전제.** 인증·인가는 미구현 기능으로 간주하고, 현 구조가 인가 추가를 방해하는 지점만 지적한다.
- 관점: 보안 / 성능(정적 분석) / 유지보수·설계 일관성 / 테스트 스위트 품질 / 인프라·운영
- 범위 밖: 발견사항 수정, 의존성 버전 안정성(alpha·RC), `.claude` 도구 적합성, 강의 원본 대비 충실도, 부하 측정
- 출처 표기: `[측정]` 직접 실행·확인 / `[문서지식]` 공식 문서 / `[추정]` 코드 기반 추론 / `[미확인]`

## 진행 방법

관점별 리뷰어 4명(보안, 성능, 유지보수·설계, 테스트)이 독립적으로 코드를 읽고 발견사항을 냈다. 그 뒤 별도 검증자가 심각도 높음 항목과 `[추정]` 항목을 적대적으로 재검증했다. 이 문서는 두 패스를 병합하고 심각도를 **운영 위험** 단일 척도로 정규화한 결과다. 원칙 위반이지만 운영 결함이 아닌 항목은 낮음으로 내렸다.

실행한 것 `[측정]`:

| 항목 | 결과 |
|---|---|
| `./gradlew test --rerun` | 스펙 31개, 테스트 162개, 실패 0, 스킵 0, 합계 10.9초 |
| `./gradlew ktlintCheck` | 위반 0 |
| `./gradlew detekt` | 발견 24건. MagicNumber 23건(전부 `ErrorCode` HTTP 상태 코드), TooManyFunctions 1건(`Curriculum` 14개 함수) |
| 보안 리뷰어의 추가 실행 | `bootJar`로 운영 jar를 만들어 로컬 기동 후 curl·로그 확인. 합의한 실행 범위(test·detekt·ktlintCheck)를 넘은 부분이라 명시한다. 저장소 파일은 변경되지 않았고 남은 프로세스는 없다. |
| 검증자의 추가 실행 | `./gradlew test --tests '*CurriculumRepositoryTest' ...` 3회. 환경변수로 JDBC·빈 팩토리 DEBUG 로깅을 켜서 SQL 문장 수와 빈 주입 결정을 실측. 파일 변경 없음. |

## 요약

| 관점 | 높음 | 중간 | 낮음 |
|---|---|---|---|
| 보안 | 2 | 6 | 3 |
| 성능 | 1 | 5 | 4 |
| 유지보수·설계 | 1 | 0 | 6 |
| 테스트 품질 | 0 | 2 | 9 |
| 인프라·운영 | 0 | 2 | 2 |
| **합계** | **4** | **15** | **24** |

**먼저 처리할 여섯 가지.** 앞의 둘은 코드 두 줄 수준이다.

1. JWT 서명 키를 설정 파일·git 이력에서 제거하고 재발급한다. (S-1)
2. 유효성 검사 실패 로그에서 거부된 값을 빼서 비밀번호가 로그에 남지 않게 한다. (S-2)
3. 아키텍처 테스트에 `doesNotDependOn` 규칙과 위반 픽스처를 추가한다. 지금은 도메인 격리 하나만 지킨다. (M-1)
4. Curriculum에 낙관적 락(`@Version`)을 넣고 충돌을 409로 매핑한다. (P-1)
5. 행위 주체(actor)를 유스케이스에 전달하는 구조를 인가 구현 전에 결정한다. 늦으면 Command 전면 수정이 된다. (S-6)
6. CI를 만들어 테스트·ktlint·detekt·시크릿 스캔을 머지 조건으로 건다. (I-1)

**진행 현황(2026-09-20):** 높음 4건(S-1, S-2, M-1, P-1) 코드 수정 완료. S-1은 키 재발급과 git 이력 제거가 저장소 밖 조치로 남아 있다. 중간 15건 중 12건(S-3, S-4, S-5, S-7, S-8, P-2 수용, P-4 부분, P-6, T-1, T-2, I-1, I-2) 완료. 나머지 3건(S-6 actor 구조, P-3 검색 페이지네이션, P-5 트랜잭션 밖 BCrypt)은 사용자 결정으로 보류 — 각각 인가 설계, Course 웹 어댑터 설계, 트랜잭션 경계 재설계 시점에 다룬다. 각 항목의 「처리 상태」 인용문 참고. 낮음 24건 중 21건 코드 수정 완료, 2건 유지·수용 결정(P-8, M-7), 1건 보류(P-7 → P-3 과 함께 Course 웹 어댑터 설계 시점). 테스트는 183개 → 209개.

---

## 1. 보안

### S-1 [높음] JWT 서명 비밀키가 운영 설정에 평문으로 커밋되어 있고 이력에 남아 있음
> **처리 상태(2026-09-20): 코드 수정 완료, 운영 조치 대기.** `application.yml`을 `${JWT_SECRET}` 기본값 없는 플레이스홀더로 바꾸고 README에 설정법을 적었다 [측정: 테스트 166개 통과, `src/`에 평문 키 없음]. 남은 것은 저장소 밖 조치 두 가지다. (1) 키 재발급: 노출된 키로 발급된 토큰은 무효화해야 한다. (2) 이력 제거: `git filter-repo --replace-text`로 커밋 `5ca58b5` 이후 이력에서 값을 지우고 강제 푸시. 협업자 전원의 재클론이 필요한 파괴적 작업이라 사용자 결정 후 실행한다.
- 증거: `src/main/resources/application.yml:19-20`
  ```yaml
  jwt:
    secret: FHhzHAYtX7ttYa8qgzyE09XK9uVUWqh6FBApgmEDpNU=
  ```
  `git log -S` 결과 커밋 `5ca58b5`에 포함. 원격은 GitHub 개인 저장소. `SecurityConfig.kt:39-44`가 이 값을 HS256 대칭키로 사용.
- 영향: HS256은 대칭키라 이 값을 아는 누구나 임의 클레임의 유효 토큰을 위조할 수 있다. 인가가 붙는 순간 전 계정 탈취 수단이 된다. 저장소 접근자(협업자·포크·유출)는 모두 키 보유자다.
- 수정 방향: `jwt.secret: ${JWT_SECRET}` 형태로 기본값 없이 환경변수 주입(미설정 시 기동 실패). 키 재발급. `git filter-repo`로 이력 제거 후 강제 푸시. 테스트 키(`src/test/resources/application.yml:6`)는 별도 값이라 유지 가능.
- 출처: [측정]

### S-2 [높음] 유효성 검사 실패 시 비밀번호 입력값이 평문으로 WARN 로그에 기록됨
> **처리 상태(2026-09-20): 완료.** 로그를 `[필드:제약코드]` 형식으로 바꿨다. `MemberApiWebMvcTest` 실행 로그에서 `rejected value` 0회, 출력 예 `유효성 검사 실패: [email:Email]` [측정].
- 증거: `src/main/kotlin/kimspring/splearn/adapter/ApiControllerAdvice.kt:40`
  ```kotlin
  log.warn { "유효성 검사 실패: ${ex.message}" }
  ```
  `MethodArgumentNotValidException.message`는 `rejected value [...]`를 포함한다. 보안 리뷰어가 운영 jar에서 실측: `... on field 'password': rejected value [Sh0rt!]; ...`
- 영향: 8자 미만·100자 초과 비밀번호(실제 비밀번호의 오타 변형인 경우가 많다)가 WARN 레벨로 로그 수집기·APM에 평문 저장된다.
- 수정 방향: `ex.message` 대신 `fieldErrors.map { "${it.field}:${it.code}" }`만 로깅.
- 출처: [측정] 보안 리뷰어가 운영 jar에서 비밀번호 필드로 실측. 검증 패스는 기존 테스트 로그에서 같은 포맷(`on field 'email': rejected value [invalid email]`, `on field 'nickname': rejected value [ab]`)을 재확인했다. 비밀번호 필드 자체는 기존 테스트에 실패 케이스가 없어 검증 패스에서는 재현하지 않았다.

### S-3 [중간] Command 검증과 도메인 불변식이 따로 놀아 정상적인 잘못된 입력이 500으로 떨어지고 ERROR 로그에 입력값이 찍힘
> **처리 상태(2026-09-20): 완료(introduction 길이 제외).** `Email.EMAIL_REGEX`, `Profile.ADDRESS_REGEX`·`MAX_ADDRESS_LENGTH`를 공개 상수로 두고 Command의 `@Pattern`·`@Size`가 참조한다(정책 단일 출처). 섹션·수업 제목은 `CURRICULUM_TITLE_MAX_LENGTH`(200) `require`. `IllegalArgumentException` → 400 안전망을 어드바이스에 추가했고, 내부 불변식 위반도 400으로 보이는 대가를 주석에 남겼다. `introduction`의 길이 상한은 비즈니스 결정이라 넣지 않았다. 검증: WebMvc 400 본문 테스트, 도메인 `titleTooLongFail` 통과 [측정].
- 증거:
  - `application/member/command/RegisterMemberCommand.kt:8` `@field:Email val email: String` (`@NotBlank`·패턴 없음) vs `domain/shared/Email.kt:14` `require(EMAIL_PATTERN.matches(address))`
  - `application/member/command/UpdateMemberInfoCommand.kt:8` `@field:Size(max = 15) val profileAddress` (`@Pattern` 없음) vs `domain/member/Profile.kt:12` `require(PROFILE_ADDRESS_PATTERN.matches(address))`. 같은 길이 정책 15가 리터럴과 `Profile.MAX_ADDRESS_LENGTH` 상수로 이중 정의.
  - `UpdateMemberInfoCommand.kt:9` `introduction`에 `@Size` 없음 vs `V1__init_schema.sql:4` `TEXT`. Curriculum 섹션·수업 `title`은 어느 계층에도 길이 검증 없음(`Section.kt`, `Lesson.kt`) vs `V8:11,20` `VARCHAR(200)`.
  - `ApiControllerAdvice.kt:80-84` `IllegalArgumentException` 핸들러 없음. `Exception` 포괄 핸들러가 500 반환.
  - 실측(보안 리뷰어): `{"email":""}` → 500, `{"email":"a@b"}` → 500, `{"profileAddress":"ABC"}` → 500. 로그 `ERROR ... 이메일 형식이 바르지 않습니다: a@b` + 스택트레이스.
- 영향: 클라이언트 입력 오류가 서버 오류로 집계돼 알람 오탐과 에러 버짓 소모. 공격자가 ERROR 스택트레이스를 임의로 대량 발생시킬 수 있다. 이메일(PII)이 ERROR 로그에 남는다. 응답 본문은 고정 메시지라 외부 노출은 없다.
- 수정 방향: (1) 어드바이스에 `IllegalArgumentException` → 400 `INVALID_INPUT` 매핑을 안전망으로 추가. (2) Command 검증은 도메인 상수를 참조하거나(`Profile.MAX_ADDRESS_LENGTH`) 형식 검증을 도메인 한 곳으로 모은다. (3) ERROR 로그에 입력값 대신 필드명만.
- 출처: [측정]

### S-4 [중간] BCrypt 72바이트 제한과 `@Size(max = 100)` 불일치로 정상 입력이 500
> **처리 상태(2026-09-20): 완료.** `support/validation/MaxByteLength` 커스텀 제약(UTF-8 바이트 수)을 만들고 가입·로그인 Command의 비밀번호에 `@Size(max = 72)`와 함께 적용했다. 검증: 한글 25자 비밀번호 → 400, `fields[0].field == password` [측정].
- 증거: `RegisterMemberCommand.kt:10` `@field:Size(min = 8, max = 100) val password`. `adapter/security/SecurePasswordEncoder.kt:9,11` `BCryptPasswordEncoder()` / `encode(password)!!`. 보안 리뷰어가 spring-security-crypto 7.0.5로 `encode("가"×25)` 실행 → `IllegalArgumentException: password cannot be more than 72 bytes`. 운영 jar 실측 → 500.
- 영향: 검증을 통과한 정상 입력(UTF-8 다바이트 25자, ASCII 73자)이 500으로 실패한다. 로그인도 같은 검증과 `matches()`를 쓰므로 401 대신 500이 나 실패 원인이 구분된다 [추정: 코드 동일성으로 추론, 미실행].
- 수정 방향: 바이트 길이 72 이하 커스텀 제약으로 400 처리.
- 출처: [측정] 가입 경로 / [추정] 로그인 경로

### S-5 [중간] Swagger UI·OpenAPI 문서가 운영 빌드에 포함되고 누구에게나 노출됨
> **처리 상태(2026-09-20): 완료.** `application-prod.yml`에서 `springdoc.api-docs.enabled`·`swagger-ui.enabled`를 끈다. README에 운영 실행 절 추가. 프로파일 없이 운영에 올리면 여전히 노출되므로 배포 스크립트가 `--spring.profiles.active=prod`를 넘겨야 한다. 검증: 정적(설정 파일) [측정], 실제 기동 확인은 하지 않음.
- 증거: `build.gradle.kts:42` `implementation("org.springdoc:...")` (developmentOnly 아님). `application.yml:22-26` 경로만 지정, `enabled` 미설정, 프로파일 분리 없음. 운영 jar 실측: `/swagger-ui/index.html` 200, `/v3/api-docs` 200.
- 영향: 전체 엔드포인트·스키마·에러코드 체계가 공개된다. `OpenApiConfig.kt:18` 설명이 "Splearn 내부 관리 API"인데 외부에 공개하는 셈.
- 수정 방향: 운영 프로파일에서 `springdoc.api-docs.enabled=false`, `springdoc.swagger-ui.enabled=false`. 노출이 의도라면 인증 매처 뒤로.
- 출처: [측정]

### S-6 [중간] 행위 주체(actor)를 받을 자리가 없고 대상 ID가 요청에서 옴. 향후 인가 추가를 구조적으로 방해
> **처리 상태(2026-09-20): 보류(사용자 결정).** 역할·소유자 규칙이 정해지지 않은 상태에서 Actor 타입과 전 유스케이스 시그니처를 바꾸는 것은 추측성 코드라 인가 설계 시점에 함께 처리한다.
- 증거:
  - `adapter/webapi/MemberApiSpec.kt:40-58` 변경 엔드포인트가 `@PathVariable memberId`만 받는다. `Authentication`·`@AuthenticationPrincipal` 파라미터 없음.
  - `application/course/command/CreateCourseCommand.kt:6` `val instructorId: Long`, `application/enrollment/command/EnrollCommand.kt:4` `val memberId: Long`. 행위자 ID를 클라이언트가 지정한다.
  - 유스케이스 인터페이스(`MemberLifecycle`, `MemberModifier`, `CoursePublisher`, `InstructorApplication`) 어디에도 "누가"라는 인자나 소유자 검증 훅이 없다.
  - 실측: 자격증명 없이 `PATCH /api/members/1/activate` → 200.
- 영향: 인가를 붙일 때 본문의 `instructorId`/`memberId`를 신뢰하면 IDOR이 그대로 남고, 제대로 하려면 모든 Command·유스케이스 시그니처를 동시에 바꿔야 한다. 관리자 동작(강사 승인, 강의 publish)과 사용자 동작이 같은 형태라 역할 구분 지점도 없다.
- 수정 방향: 어댑터가 principal을 `Actor(memberId, roles)` 같은 애플리케이션 타입으로 변환해 유스케이스 첫 인자로 전달. Command에서 행위자 ID를 제거하고 대상 ID만 남긴다. 서비스 진입부에 소유자 검증 훅을 둔다.
- 출처: [측정]

### S-7 [중간] 회원 활성화(이메일 인증 완료 단계)가 추측 가능한 순번 ID만으로 수행됨
> **처리 상태(2026-09-20): 완료.** `Member.activationToken`(가입 시 UUID 생성, 활성화 시 소비), 가입 후 24시간 만료(`ACTIVATION_VALID_HOURS`), `InvalidActivationTokenException`(M006, 400), V10 `member.activation_token` NULL 컬럼 + 유니크 인덱스. 가입 메일 본문에 `/api/members/activate?token=` 경로를 싣고, 엔드포인트를 `POST /api/members/activate?token=`으로 교체(`PATCH /{id}/activate` 제거). 검증: 도메인(토큰 소비·만료), 유스케이스(잘못된 토큰·재사용), WebMvc(200·400 M006), 통합(가입→토큰→ACTIVE), 리포지토리(토큰 조회) 테스트 통과 [측정]. 계약 테스트(404·403·500·409)는 deactivate 엔드포인트로 옮겨 유지.
- 증거: `MemberApiSpec.kt:40-43` `@PatchMapping("/{memberId}/activate")`. `MemberModifyService.kt:72` 환영 메일 본문에 토큰이 없다. `V1__init_schema.sql:13` `member.id BIGINT AUTO_INCREMENT`. 실측: 가입 직후 `PATCH /api/members/1/activate` → 200 ACTIVE.
- 영향: 이메일 소유 확인이라는 활성화의 목적이 무력화된다. 인증이 붙어도 "본인이 자기 계정을 활성화"하는 흐름은 이메일 검증 토큰이 있어야 성립한다. 타인 이메일로 가입 후 순번 순회로 활성화 가능.
- 수정 방향: 메일에 실린 1회용·만료되는 난수 토큰으로만 활성화. 토큰-회원 매핑 저장.
- 출처: [측정]

### S-8 [중간] 이메일(PII)이 INFO/WARN/ERROR 로그에 반복 기록됨
> **처리 상태(2026-09-20): 완료.** `Email` 형식 예외와 `DuplicateEmailException` 메시지에서 주소를 제거했고, `DummyEmailSender`는 제목만 로그에 남긴다. `DuplicateKeyException` 핸들러(P-6)도 DB 메시지를 로그에 싣지 않는다. 검증: `src/main`에 이메일 값 보간 없음(grep) [측정]. 프로필 주소는 공개 핸들이라 메시지에 남겼다.
- 증거: `adapter/integration/DummyEmailSender.kt:19` `log.info { "DummyEmailSender.send email: $email" }`. `MemberModifyService.kt:77` `"이미 사용중인 이메일입니다: ${command.email}"` → `ApiControllerAdvice.kt:29` WARN 출력. S-3의 ERROR 로그.
- 영향: 로그 보존 기간 동안 이메일이 평문 축적된다. `DummyEmailSender`는 `@Fallback`이라 운영에 실제 구현이 없으면 그대로 동작한다.
- 수정 방향: 예외 메시지에서 이메일 제거(ID 또는 마스킹). `MemberModifyService.kt:36`처럼 `memberId`만 남기는 패턴으로 통일.
- 출처: [측정]

### S-9 [낮음] 운영 클래스패스에 H2가 있고 데이터소스 미설정 시 임베디드 H2로 폴백
> **처리 상태(2026-09-20): **코드 수정 완료.** `runtimeOnly("com.h2database:h2")` → `testRuntimeOnly`. 운영 데이터소스는 S-3 처리 때 만든 `application-prod.yml` 의 기본값 없는 플레이스홀더가 담당한다. 데이터소스 미설정 기동 시 Spring Boot 가 "Failed to configure a DataSource" 로 즉시 실패하는 동작은 [문서지식], jar 재기동 실측은 하지 않았다 [미확인].**
- 증거: `build.gradle.kts:40` `runtimeOnly("com.h2database:h2")` (testRuntimeOnly 아님). `application.yml`에 `spring.datasource.*` 없음. compose 연동은 `developmentOnly`라 운영 jar 미포함. 인자 없이 기동 실측: `HikariPool-1 ... url=jdbc:h2:mem:... user=SA` → Flyway V1 `Unknown data type: "DATETIME"`으로 기동 중단. `MODE=MySQL` H2 URL만 넘기면 완전히 기동됨.
- 영향: 오늘은 크래시라는 안전한 실패로 끝나지만 우연이다. 배포 설정 오류로 잘못된 URL이 들어가면 인메모리 DB 위에서 정상처럼 동작하다 재시작 시 전 데이터가 사라진다.
- 수정 방향: H2를 `testRuntimeOnly`로. 운영 데이터소스는 기본값 없는 플레이스홀더로 명시해 미설정 시 즉시 실패.
- 출처: [측정] 보안 리뷰어 jar 기동 / 검증 패스: 의존성 스코프·datasource 부재 정적 재확인 CONFIRMED. Spring Boot 문서 "필요한 빌드 의존성이 있으면 명시적 URL 없이 임베디드 DB를 자동 구성" [문서지식]

### S-10 [낮음] JWT 디코더에 issuer/audience 검증이 없음
> **처리 상태(2026-09-20): **코드 수정 완료.** `jwt.issuer`·`jwt.audience` 설정(기본값 `splearn`/`splearn-api`, `JWT_ISSUER`/`JWT_AUDIENCE` 로 변경)을 추가하고 `JwtValidators.createDefaultWithIssuer` + `aud` 클레임 `JwtClaimValidator` 를 디코더에 연결했다. `MemberApiTest.rejectsTokenOfAnotherIssuerOrAudience`(같은 키로 서명한 다른 iss·aud 토큰 → 401), `acceptsTokenOfThisService`(→ 201) 로 고정 [측정]. 트레이드오프: `secret` 은 기본값 없이 fail-fast 인데 `issuer`/`audience` 는 기본값을 둔다 — 비밀이 아니고 발급 주체가 아직 없어서다. 발급 구현 시 fail-fast 로 통일할지 결정한다.**
- 증거: `SecurityConfig.kt:41-44` `NimbusJwtDecoder.withSecretKey(secretKey).macAlgorithm(HS256).build()`. `jwtValidator` 미설정. 토큰 발급 코드 없음.
- 영향: 기본 검증기는 만료만 본다 [문서지식]. 같은 키로 서명된 다른 용도의 토큰이 수용된다. 지금은 발급 주체가 없어 실해는 없지만 발급 구현 시 잊기 쉬운 지점.
- 수정 방향: 발급 구현과 함께 `JwtValidators.createDefaultWithIssuer(...)` + audience 검증기.
- 출처: [측정] 설정 부재 / [문서지식]

### S-11 [낮음] 이메일 대소문자 정규화 정책이 없고 테스트 DB와 운영 DB의 비교 의미가 다름
> **처리 상태(2026-09-20): **코드 수정 완료.** `Email` 이 생성 시 소문자로 정규화한다(생성자 private + `companion invoke`). `EmailTest.normalizesCase`, `MemberRegisterTest.duplicateEmailFailIgnoringCase`(`Kim.Lee@Splearn.App` 가입 후 소문자 재가입 → `DuplicateEmailException`) 로 고정 [측정]. 트레이드오프: `ArchUnitConditions` 의 조회 전용 규칙에 `Email.Companion.invoke` 호출만 생성자와 같은 취급으로 허용했다(값 객체 생성은 상태 전이가 아니다). 리뷰 패스 지적으로 `\$Companion` 이름 기반 전역 허용에서 클래스 명시 목록으로 좁혔다. `operator invoke` 는 저장소에서 `Email` 이 유일한 사례이며, 같은 패턴의 VO 가 늘면 이름 있는 팩터리(`of`)로 통일할지 재검토한다. 강의 제목은 정규화하지 않는다 — 대소문자만 다른 제목을 같은 강의로 볼 근거가 없고, `findByTitleContaining` 의 DB별 차이는 P-3 검색 설계 때 함께 다룬다.**
- 증거: `Email.kt:3-17` 소문자 변환 없음. `src/test/resources/application.yml:3` H2 URL에 `IGNORECASE` 없음(데이터 비교는 대소문자 구분). compose·마이그레이션에 콜레이션 지정 없음 → MySQL 8 기본 `utf8mb4_0900_ai_ci`(대소문자 무시) [문서지식].
- 영향: 두 리뷰어가 "운영에서 409 대신 500"이라 주장했으나 **이 부분은 기각한다.** MySQL에서는 사전 조회 `WHERE email_address = ?`도 같은 콜레이션으로 비교되므로 `Kim@x.com`은 `kim@x.com`과 매칭되어 409가 정상 반환된다. 실제 문제는 (1) H2 테스트에서는 둘 다 가입 성공이라 운영 동작을 테스트로 보증할 수 없고, (2) "이메일 대소문자를 구분하는가"라는 정책이 도메인에 명시되어 있지 않으며 DB 기본값에 맡겨져 있다는 점이다. `findByTitleContaining`의 LIKE도 같은 이유로 DB마다 결과가 다르다.
- 수정 방향: `Email` 생성 시 소문자 정규화를 도메인 정책으로 명시하고 테스트로 고정. 강의 제목 정규화 여부도 결정.
- 출처: [측정] 코드·설정 / [문서지식] H2 문서 "MODE=MySQL이어도 텍스트 비교는 대소문자 구분, `SET IGNORECASE TRUE`를 별도로 설정해야 함", MySQL 8 기본 콜레이션 / [추정] 운영 동작(MySQL 실측 없음). 검증 패스: `MemberRepositoryTest.duplicateEmailFail`과 `CourseRepositoryTest.uniqueTitleAndInstructor`가 완전히 같은 문자열만 쓰는 것을 확인. CONFIRMED(분석적).

---

## 2. 성능 (정적 분석)

### P-1 [높음] Curriculum 편집에 낙관적 락이 없어 동시 편집이 조용히 뒤섞임
> **처리 상태(2026-09-20): 완료.** `Curriculum.version`(도메인) ↔ `CurriculumJdbcEntity.@Version` 매핑, `V9__add_curriculum_version.sql`, `ErrorCode.CONCURRENT_MODIFICATION`(409, C006), `ApiControllerAdvice.handleOptimisticLocking` 추가. Spring Data JDBC 4.0.5는 버전 불일치 시 `OptimisticLockingFailureException`을 래핑 없이 던진다(소스 확인). 검증 테스트 `staleSaveFailsWithOptimisticLock`(같은 버전을 읽은 두 편집 중 나중 저장이 실패), `activateConflictOnConcurrentModification`(409 + C006) 통과 [측정]. 나머지 애그리거트(P-9)는 미적용.
- 증거: `grep -rn "@Version" src/main` → 없음. `domain/curriculum/Curriculum.kt:12-15,38-47` `addSection(sectionIndex, ...)`, `removeSection(sectionIndex)` 모두 위치 인덱스 기반. `application/curriculum/CurriculumModifyService.kt:75` read-modify-write가 행 락 없이 SELECT 후 저장.
- 영향: 두 사용자가 같은 커리큘럼을 동시에 편집하면 나중 `save()`가 앞선 결과를 통째로 덮어쓴다(P-2의 전체 재삽입이므로 병합이 아니라 대체). 예: A가 섹션 0 삭제, B가 섹션 1에 수업 추가 → B의 저장이 A의 삭제를 되돌리거나 B의 수업이 의도와 다른 섹션에 들어간다. 사용자 1명이 편집하는 도구라도 탭 두 개·재시도 요청으로 재현된다.
- 수정 방향: `CurriculumJdbcEntity`에 `@Version val version: Long? = null` + V9 마이그레이션. `OptimisticLockingFailureException`을 `ApiControllerAdvice`에서 409로 매핑. Member/Course/Enrollment/Instructor는 상태 전이가 status 검사로 보호돼 영향이 작다(P-9).
- 출처: [측정] / [문서지식] Spring Data JDBC optimistic locking

### P-2 [중간] Curriculum 저장 1건이 섹션·수업 트리 전체를 삭제·재삽입함
> **처리 상태(2026-09-20): 수용.** 수정 방향 (1)대로 현재 규모에서는 받아들이고, `CurriculumJdbcEntity`의 매핑 위에 동작(수업 1개 추가 = JDBC 호출 6회, id 보존)과 재검토 조건(편집 빈도 증가 시 애그리거트 분리 또는 변경분 UPDATE)을 주석으로 남겼다. SQL 문장 수를 고정하는 테스트는 데이터소스 프록시 없이는 측정할 수 없어 넣지 않았다.
- 증거: `adapter/out/persistence/CurriculumJdbcEntity.kt:12-13` `@MappedCollection(idColumn = "curriculum", keyColumn = "section_order") val sections: List<SectionJdbcEntity>`. `SectionJdbcEntity.kt:12-13` 동일 패턴으로 `lessons`. `CurriculumModifyService.kt:75` 모든 편집이 `getById` → 변경 → `save()`.
- 영향: 수업 제목 하나를 바꾸는 `updateLessonTitle`도 루트 UPDATE + 전체 lesson DELETE + 전체 section DELETE + 섹션 수·수업 수 만큼 INSERT를 유발한다. 섹션 10개·수업 100개 규모라면 편집 1회에 약 110행 삭제·재삽입. 드래그 정렬처럼 편집이 잦은 강사 도구에서 쓰기 부하와 락 유지 시간이 커진다. 자식 id는 `CurriculumRepositoryTest.keepsSectionAndLessonIdsAcrossResave`가 보존을 검증하므로 정확성 문제는 아니다 [측정].
- 검증 패스 실측 `[측정]`: JDBC DEBUG 로깅을 켜고 `CurriculumRepositoryTest`만 실행. `keepsSectionAndLessonIdsAcrossResave`의 재저장 1회(섹션 2개·수업 3개에 수업 1개 추가)에서 실행된 문장:

  | 순서 | 문장 | 비고 |
  |---|---|---|
  | 1 | `UPDATE "curriculum" SET "COURSE_ID" = ? WHERE ...` | 변경 없는데도 항상 실행 |
  | 2 | `DELETE FROM "lesson" WHERE "lesson"."section" IN (SELECT ... WHERE "section"."curriculum" = ?)` | 수업 3행 삭제 |
  | 3 | `DELETE FROM "section" WHERE "section"."curriculum" = ?` | 섹션 2행 삭제 |
  | 4 | `INSERT INTO "section" ("curriculum", "ID", "section_order", "TITLE")` batch | 기존 id 포함 재삽입 |
  | 5 | `INSERT INTO "lesson" ("lesson_order", "section", "TITLE")` | 신규 수업 1행 |
  | 6 | `INSERT INTO "lesson" ("ID", "lesson_order", "section", "TITLE")` batch | 기존 id 포함 재삽입 |

  수업 1개 추가 = JDBC 호출 6회, 자식 5행 삭제 후 6행 재삽입. 4·6번 문장에 `"ID"` 컬럼이 있어 기존 자식 id는 보존된다. batch INSERT는 적용된다.
- 수정 방향: 현재 규모에서는 수용하고 SQL 문장 수를 통합 테스트로 고정한다. 편집이 잦아지면 Section/Lesson을 별도 애그리거트로 분리하거나 어댑터에서 변경분만 UPDATE하는 커스텀 SQL을 둔다.
- 출처: [측정] 매핑·서비스 / [문서지식] Spring Data JDBC "기존 애그리거트는 참조 엔티티 삭제 → 루트 업데이트 → 재삽입"

### P-3 [중간] `findByTitleContaining`: 앞 와일드카드 LIKE 풀스캔 + 무한 리스트 + 건당 자식 조회
> **처리 상태(2026-09-20): 보류(사용자 결정).** 현재 HTTP로 도달 불가. 페이지 크기·커서 방식은 API 설계 결정이라 Course 웹 어댑터를 만들 때 함께 정한다.
- 증거: `adapter/out/persistence/SpringCourseRepository.kt:6` `fun findByTitleContaining(keyword: String): List<CourseJdbcEntity>`. `V6__create_course.sql:4,7` `title`은 `UK_COURSE_INSTRUCTOR_ID_TITLE(instructor_id, title)`의 두 번째 컬럼일 뿐 단독 인덱스 없음. `CourseJdbcEntity.kt:14` `val detail: CourseDetailJdbcEntity` (1:1 참조 엔티티).
- 영향: `Containing`은 `LIKE '%kw%'`로 번역되어 인덱스를 못 탄다 → 풀스캔. 결과 건수 상한이 없어 한 글자 검색이면 강의 전체가 반환되고, 루트 N건마다 `course_detail` 조회가 따로 나가 1+N이 된다. 현재는 웹 어댑터가 없어 HTTP로 도달 불가하다. **검색 API를 노출하는 시점에 높음으로 승격한다.**
- 수정 방향: (1) `Pageable`/`Slice` 파생 쿼리로 상한. (2) `StartingWith` + `title` 인덱스, 또는 부분 일치 필수면 MySQL FULLTEXT + `@Query`. (3) 목록 조회는 detail을 빼는 `@Query` 프로젝션.
- 출처: [측정] / [문서지식] Containing 번역, 참조 엔티티 별도 로딩 / [추정] 루트 행마다 자식 쿼리 1회

### P-4 [중간] 회원 가입 트랜잭션 안에서 외부 호출(EmailSender)이 실행됨
> **처리 상태(2026-09-20): 부분 완료.** application 계층은 Spring import가 0건인 관례를 지키기 위해, 어댑터 `DummyEmailSender`가 트랜잭션 동기화가 활성이면 `afterCommit`에 발송을 미룬다. `EmailSender` 포트 KDoc에 "구현체는 커밋 후 발송" 규칙을 명시했다. 지연 등록은 `DummyEmailSenderTest.defersSendingWhileTransactionActive`로 검증 [측정]. **해소된 것:** 발송 실패가 가입을 롤백시키는 문제, 발송 중 `member` 행 락 유지. **남은 것:** 별도 리뷰 패스가 spring-tx 7.0.7 소스로 확인한 대로 `afterCommit`은 `cleanupAfterCompletion`(커넥션 반납) **앞**에 실행되므로, 발송 동안 커넥션은 여전히 점유된다. 완전한 해소는 콜백 안에서 별도 스레드로 넘기거나(`@Async`) 도메인 이벤트 + `@TransactionalEventListener`로 옮겨야 하며, 이는 P-5와 같은 "트랜잭션 경계 재설계" 결정에 묶어 사용자 판단 대기.
- 증거: `MemberModifyService.kt:34-35` `val saved = memberRepository.save(member); sendWelcomeEmail(saved)`. 클래스는 `@ApplicationService` = `@Transactional`(`support/stereotype/ApplicationService.kt:11`).
- 영향: 현재는 `DummyEmailSender`(로그만)라 무해하다. 실제 SMTP/HTTP 발송기로 교체되는 순간 외부 지연만큼 DB 커넥션과 `member` 행 락이 붙잡히고, 발송 실패가 가입 자체를 롤백시킨다. HikariCP 기본 풀 10개(I-2)와 결합하면 가입 API가 메일 서버 속도에 묶인다.
- 수정 방향: `@TransactionalEventListener(AFTER_COMMIT)` 또는 `TransactionSynchronization.afterCommit`으로 커밋 후 발송.
- 출처: [측정] / 검증 패스: 스테레오타입·클래스 애너테이션·호출 위치 재확인 CONFIRMED

### P-5 [중간] BCrypt 해싱이 트랜잭션(커넥션 점유) 안에서 수행됨
> **처리 상태(2026-09-20): 보류(사용자 결정).** `NOT_SUPPORTED`로 빼면 `@Transactional` 통합 테스트가 준비한 미커밋 데이터를 못 본다. 비트랜잭션 스테레오타입 신설과 테스트 패턴 변경이 함께 필요한 설계 결정이라 P-4 잔여(커넥션 점유)와 묶어 다음에 다룬다.
- 증거: `MemberModifyService.kt:33` `Member.register(..., passwordEncoder, ...)` → `Member.kt:58` `encoder.encode(...)`. 메서드 전체가 `@Transactional`. `MemberAuthenticationService.kt:12,23` `@QueryApplicationService`(readOnly 트랜잭션) 안에서 `verifyPassword`.
- 영향: `DataSourceTransactionManager`는 트랜잭션 시작 시 커넥션을 획득하므로 [문서지식], 의도적으로 느린 BCrypt 연산 동안 풀 커넥션이 유휴 점유된다. 로그인·가입 동시 요청이 풀 크기(기본 10)를 넘으면 DB는 놀고 있는데 커넥션 대기가 발생한다.
- 수정 방향: 인증 서비스는 단일 SELECT라 트랜잭션 자체가 불필요하다. 가입은 해시를 먼저 계산해 전달하거나 트랜잭션 범위를 저장으로 좁힌다.
- 출처: [측정] / [문서지식]

### P-6 [중간] check-then-act 경합 시 DB 유니크 위반이 500으로 새어나옴 (데이터는 보호됨)
> **처리 상태(2026-09-20): 완료.** `ErrorCode.DUPLICATE_RESOURCE`(409, C007)와 `ApiControllerAdvice.handleDuplicateKey` 추가. `DataIntegrityViolationException` 전체가 아니라 유니크 위반 전용 하위 타입 `DuplicateKeyException`만 409로 매핑해 길이 초과 같은 다른 무결성 위반은 500으로 남긴다. 검증: WebMvc `registerConflictOnDuplicateKey` → 409 + C007 [측정].
- 증거: 애플리케이션 중복 검사 5곳(`MemberModifyService.kt:66,76`, `InstructorModifyService.kt:32`, `EnrollmentModifyService.kt:37`, `CourseValidationService.kt:24,33`). 대응 DB 제약 5개(`V1:9,20`, `V5:6`, `V6:7`, `V7:9`). `ApiControllerAdvice.kt:80-84` `DataIntegrityViolationException`·`DbActionExecutionException` 핸들러 없음.
- 영향: 유니크 제약이 있어 경합 시에도 중복 행은 생기지 않는다(좋음). 다만 두 요청이 동시에 검사를 통과하면 나중 INSERT가 `DbActionExecutionException`으로 실패하고 409 대신 500 + ERROR 스택트레이스가 남는다. 선착순 수강 신청처럼 동시 클릭이 잦은 곳에서 에러 로그 노이즈와 잘못된 응답 코드.
- 수정 방향: 어댑터에서 `DataIntegrityViolationException`을 도메인 Duplicate 예외로 번역, 또는 어드바이스에서 409 매핑.
- 출처: [측정] / [문서지식]

### P-7 [낮음] `findByInstructorId` / `findByMemberId` 무한 리스트 반환
> **처리 상태(2026-09-20): **보류(사용자 결정 대기).** P-3 과 같은 페이지네이션 설계 문제라 Course 웹 어댑터 설계 시점에 함께 다룬다. 이번에 손대지 않은 유일한 성능 항목이다.**
- 증거: `SpringCourseRepository.kt:8`, `SpringEnrollmentRepository.kt:6`. 포트도 `List` 고정(`CourseRepository.kt:14`, `EnrollmentRepository.kt:12`).
- 영향: 인덱스는 있다(V6:7, V7:9 선행 컬럼). 결과 크기 상한이 없을 뿐이다. 회원 1명의 수강 이력은 무한 누적되고 Course 목록은 건당 detail 조회가 붙는다.
- 수정 방향: 포트 시그니처에 페이지/커서 파라미터.
- 출처: [측정]

### P-8 [낮음] Course·Member 저장마다 1:1 detail 행을 삭제·재삽입하며 새 id를 소비
> **처리 상태(2026-09-20): **수용.** 리뷰가 제시한 두 선택지 중 "현행 유지하고 인지" 를 택했다. `MemberJdbcEntity`·`CourseJdbcEntity` 의 `detail` 프로퍼티에 삭제·재삽입 비용과 `@Embedded` 전환 조건을 주석으로 남겼다. 쓰기 빈도가 문제가 되면 스키마 변경과 함께 다시 꺼낸다.**
- 증거: `CourseDetailJdbcEntity.kt:25-31`, `MemberDetailJdbcEntity.kt:28-35` `from()`이 `id`를 넘기지 않음(항상 null). 호출 `CourseModifyService.kt:38,44,50,54`, `MemberModifyService.kt:42,47,56`.
- 영향: 상태 전이 1회 = 루트 UPDATE + detail DELETE + detail INSERT. AUTO_INCREMENT 소모, 같은 유니크 키를 한 트랜잭션에서 지웠다 넣는다. 지금은 3배 쓰기 정도.
- 수정 방향: 현행 유지하고 인지만 하거나, `@Embedded`로 detail 컬럼을 부모 테이블에 합쳐 UPDATE 1문장(스키마 변경 필요).
- 출처: [측정] / [문서지식]

### P-9 [낮음] Member/Course/Enrollment/Instructor 상태 전이에 `@Version` 없음
> **처리 상태(2026-09-20): **코드 수정 완료.** Member·Instructor·Course·Enrollment 에 `version: Long?` 과 `~JdbcEntity` `@Version` 을 추가하고 `V11__add_version_to_aggregates.sql` 로 컬럼을 만들었다. 네 리포지토리 테스트 각각 `staleSaveFailsWithOptimisticLock`(같은 버전을 읽은 두 저장 중 나중 것 → `OptimisticLockingFailureException`) 추가 [측정]. 409 매핑은 P-1 때 만든 `ApiControllerAdvice.handleOptimisticLocking` 을 그대로 쓴다.**
- 증거: `CourseModifyService.kt:47-51` publish는 `getById` → 상태 검사 → `save`.
- 영향: 동시 `publish` 두 건이 모두 `IN_REVIEW`를 읽으면 둘 다 성공하고 `publishedAt`이 덮인다. 상태 자체는 동일해 실질 피해가 작다.
- 수정 방향: P-1 도입 시 같은 패턴을 나머지 루트에도 확장.
- 출처: [측정]

### P-10 [낮음] `section.curriculum`, `lesson.section`이 명시적 인덱스 없이 FK 자동 인덱스에 의존
> **처리 상태(2026-09-20): **코드 수정 완료.** `V12__add_curriculum_child_indexes.sql` 로 `section (curriculum, section_order)`, `lesson (section, lesson_order)` 복합 인덱스를 명시했다. H2 마이그레이션 통과 [측정], MySQL 실측 없음 [미확인]. FK 자동 인덱스는 남아 있어 중복이지만 이름이 자동 생성이라 삭제하지 않았다.**
- 증거: `V8__create_curriculum.sql:15,24` FK만 선언.
- 영향: InnoDB는 FK 컬럼에 인덱스를 자동 생성하므로 [문서지식] 현재는 자식 로딩이 인덱스를 탄다. FK를 제거하는 마이그레이션이 들어오면 인덱스도 사라진다. 정렬 컬럼이 인덱스에 없어 ORDER BY는 filesort지만 건수가 적어 무시 가능.
- 수정 방향: `(curriculum, section_order)`, `(section, lesson_order)` 복합 인덱스 명시.
- 출처: [측정] / [문서지식]

---

## 3. 유지보수·설계 일관성

### M-1 [높음] 아키텍처 테스트가 실제로 강제하는 계층 규칙은 `domain.dependsOnNothing()` 하나뿐
> **처리 상태(2026-09-20): 완료.** (1) `application.doesNotDependOn(adapter)` 추가. (2) ArchUnit `slices().matching("kimspring.splearn.adapter.(*)..").should().notDependOnEachOther()` 테스트 추가. (3) `KonsistLearningTest`에 `MyAdapter → MyService2` 실제 의존을 `doesNotDependOn`으로 단언하면 `KoAssertionFailedException`이 나는 음성 케이스 추가. (4) 조회 허용 조건을 「`ensure` 접두사, 또는 인자 없는 `^(get|is)[A-Z]` 접근자」로 교체. 인자를 받는 `getOrCreate(x)`류는 더 이상 통과하지 않는다. 인자 없이 상태를 바꾸는 get 메서드는 이름 기반 판정의 한계로 여전히 못 막는다. (5) 별도 리뷰 패스가 잡은 공백 보정: `(*)` 슬라이스 매칭은 `adapter` 루트 직속 `ApiControllerAdvice`를 제외하므로, 루트 직속 클래스가 `out`·`security`·`integration`에 의존하지 못하게 하는 `noClasses()` 규칙을 추가했다(개발가이드 48행이 위치를 루트로 규정해 파일은 옮기지 않음). 신규 테스트 3건 포함 167개 전부 통과 [측정].
- 증거:
  - `src/test/kotlin/kimspring/splearn/HexagonalArchitectureTest.kt:24-26`
    ```kotlin
    domain.dependsOnNothing()
    application.dependsOn(domain)
    adapter.dependsOn(domain, application)
    ```
  - Konsist 0.17.3 소스(`core/verify/KoArchitectureAssert.kt`)를 Gradle 캐시에서 풀어 확인 [측정]: 실패를 만드는 경로는 (a) `strict = true`인 `dependsOn`에 실제 의존이 없을 때, (b) `doesNotDependOn` 위반, (c) `dependsOnNothing` 위반 세 가지다. 비-strict `dependsOn(...)`(기본값)은 "있어도 된다" 선언일 뿐 허용 목록이 아니다. 따라서 **application이 adapter를 import해도, adapter.webapi가 adapter.out.persistence를 import해도 이 테스트는 통과한다.** 같은 레이어 내부 의존은 어떤 규칙으로도 검사되지 않는다(`getDependentOnAnyLayerFiles`가 자기 레이어 import를 명시적으로 제외).
  - `support/archunit/ArchUnitConditions.kt`의 두 조건은 도메인 슬라이스 간 호출과 어댑터→도메인 호출만 검사.
  - `learningtest/archunit/KonsistLearningTest.kt:10-21` 통과 케이스만 존재. `ArchUnitSliceLearningTest.kt:11` 주석 "성공 케이스만 통과하는 테스트는 규칙이 비어 있어도 통과한다"는 원칙이 Konsist 쪽엔 적용되지 않았다.
  - `ArchUnitConditions.kt:47,55` 허용 규칙이 접두사(`get`, `is`, `ensure`) `startsWith` 기반. 생성자 호출은 수집하지 않음.
  - 현재 코드에는 adapter→adapter 의존이 없음을 grep으로 확인 [측정].
- 영향: 운영 결함은 아니다. 그러나 CLAUDE.md와 `.claude/skills/architecture/SKILL.md`가 "절대 금지"로 못 박은 application→adapter, adapter→adapter 의존이 생겨도 테스트가 통과한다. 프로젝트가 아키텍처 안전망이라 믿는 테스트가 사실상 도메인 격리 하나만 지킨다. Layer 패턴 오타도 잡히지 않고, `getOrCreateX()` 같은 변경 메서드가 접두사로 통과한다. 이 상태로 협업자·AI 에이전트가 코드를 추가하면 회귀를 감지할 수단이 없다. 그래서 높음으로 둔다.
- 수정 방향: (1) `application.doesNotDependOn(adapter)`, `domain.doesNotDependOn(application, adapter)`를 명시. (2) 어댑터 하위 패키지는 ArchUnit `noClasses().that().resideInAPackage("..adapter.webapi..").should().dependOnClassesThat().resideInAPackage("..adapter.out..")` 류 규칙. (3) learningtest에 application→adapter import 위반 픽스처를 두고 `shouldThrow`로 규칙이 실제로 실패함을 증명. (4) 접두사 규칙을 정규식 `^(get|is)[A-Z]|^ensure`로 조임.
- 출처: [측정] Konsist 소스 / [측정] 현재 코드에 위반 사례 없음(grep)

### M-2 [낮음] `CourseValidationService.validateForCreate`와 `Course.create`가 같은 규칙을 두 곳에서 검증
> **처리 상태(2026-09-20): **코드 수정 완료.** `CourseValidationService.validateForCreate` 에서 `instructor.ensureActive()` 를 제거해 강사 상태 규칙의 단일 출처를 `Course.create` 로 만들었다. `CourseCreatorTest.createFailPendingInstructor` 가 도메인 검사가 죽은 코드가 아님을 증명한다 [측정].**
- 증거: `application/course/CourseValidationService.kt:22` `instructor.ensureActive()` / `domain/course/Course.kt:47` `instructor.ensureActive()`. `CourseModifyService.kt:26-27`이 검증 서비스를 먼저 호출한 뒤 `Course.create`를 호출.
- 영향: 이 경로에서 `Course.create` 안의 검사는 항상 통과하는 죽은 코드다. 규칙의 단일 출처가 도메인이어야 한다는 원칙 위반. 운영 결함은 아니다.
- 수정 방향: 검증 서비스에서 `ensureActive()` 제거. 검증 서비스는 도메인이 표현할 수 없는 규칙(제목 중복)만 담당.
- 출처: [측정]

### M-3 [낮음] `SecurityConfig`가 필드 주입(`@Value` + `lateinit var`) 사용
> **처리 상태(2026-09-20): **코드 수정 완료.** `SecurityConfig(@param:Value(...) private val secret, issuer, audience)` 생성자 주입. 저장소의 프로덕션 `lateinit var` 는 0건 [측정].**
- 증거: `adapter/security/SecurityConfig.kt:20-21`. 저장소에서 `lateinit var` 프로덕션 사용처는 이 한 곳.
- 영향: 프로젝트 규칙(생성자 주입, `lateinit var` 금지) 위반. 생성자 시점에 값이 없어도 컴파일이 통과한다.
- 수정 방향: `class SecurityConfig(@Value("\${jwt.secret}") private val secret: String)`.
- 출처: [측정]

### M-4 [낮음] `*Fixture`가 하드코딩 PK 기본값(`id = 1L`) 사용
> **처리 상태(2026-09-20): **코드 수정 완료.** 네 픽스처의 `id = 1L` 기본값을 `MemberFixture.randomId()`(Instancio 랜덤 Long) 로 바꿨다. `createMember(1L)` 처럼 테스트가 명시적으로 넘기는 값은 그대로 두었다.**
- 증거: `CourseFixture.kt:45-46`, `InstructorFixture.kt:13`, `CurriculumFixture.kt:7`, `MemberFixture.kt:48`.
- 영향: 규칙(하드코딩 PK 금지) 위반이지만 순수 인메모리 도메인 단위 테스트에서만 쓰이고, DB 저장 테스트는 id 없는 픽스처를 써서 실제 채번한다(`BaseRepositoryTest.kt:53-59`). 충돌 위험 없음.
- 수정 방향: 우선순위 낮음. 시퀀스 카운터나 Instancio 랜덤 Long으로 교체 가능.
- 출처: [측정]

### M-5 [낮음] `*Finder.find(id)`가 명명 규칙과 달리 예외를 던짐
> **처리 상태(2026-09-20): **코드 수정 완료.** 리네이밍을 택했다 — 규칙에 예외를 적는 것보다 규칙을 지키는 편이 grep 으로 검증 가능하다. 5개 `~Finder.find(id)` → `get(id)`, `CurriculumFinder.findByCourse` → `getByCourse`(비null 반환·예외 throw 였으므로 같은 위반). 개발가이드·test SKILL 갱신.**
- 증거: `CourseFinder.kt:9` `fun find(courseId: Long): Course` → `CourseQueryService.kt:12` `getById`. `MemberFinder`, `InstructorFinder`, `EnrollmentFinder`, `CurriculumFinder` 동일.
- 영향: 포트(`*Repository`) 레벨은 `find`/`get`이 정확히 구분되어 있으나 유스케이스 레벨은 `find`가 throw한다. 5개 모두 일관된 선택이라 혼란 흔적은 없다.
- 수정 방향: 문서에 "Finder의 대표 조회는 예외를 던진다" 예외 규칙을 명시하거나 `get()`으로 리네이밍.
- 출처: [측정]

### M-6 [낮음] 소스 트리 안에 도구 세션 상태 디렉터리 존재
> **처리 상태(2026-09-20): **완료.** `src/main`·`src/test` 안의 `.omc/` 두 개 삭제. 루트 `.omc/` 는 해당 도구의 프로젝트 상태라 유지했다. 도구가 소스 패키지 아래에 상태를 만들지 않도록 하는 설정은 저장소 밖 조치.**
- 증거: `src/main/kotlin/kimspring/splearn/.omc/state/sessions/*/pre-tool-advisory-throttle.json`, `src/test/kotlin/kimspring/splearn/.omc/...`. `.gitignore:41`로 무시되어 추적 파일은 0개 [측정].
- 영향: git 이력에는 없다. 소스 패키지 디렉터리에 무관한 파일이 물리적으로 섞여 있어 다른 도구 스캔에 잡음.
- 수정 방향: 삭제. 해당 도구의 상태 경로를 프로젝트 루트로.
- 출처: [측정]

### M-7 [낮음] `learningtest` 패키지가 운영 테스트와 같은 태스크에서 실행됨
> **처리 상태(2026-09-20): **유지 결정.** 리뷰 결론대로 현행 유지. `ArchUnitSliceLearningTest` 는 규칙 실효성 회귀 테스트 역할이 있고, 나머지 두 스펙의 비용은 1.4초 수준이다.**
- 증거: `build.gradle.kts:58-60` 필터 없음. `KonsistLearningTest.kt:12` `scopeFromTest()`가 테스트 소스 전체를 파싱. XML 기준 3스펙 합계 1.43초 / 전체 10.9초 [측정].
- 영향: 프로젝트와 무관한 가짜 클래스 기반 테스트가 리포트에 섞인다. 단 `ArchUnitSliceLearningTest`는 규칙 실효성 회귀 테스트 역할이라 남기는 것이 타당.
- 수정 방향: 현재 유지 가능. 커지면 Kotest 태그로 `InstancioLearningTest`만 분리.
- 출처: [측정]

---

## 4. 테스트 스위트 품질

### T-1 [중간] 에러 응답 계약(`ErrorResponse` 필드, 404/405/403/500)이 어느 테스트에서도 검증되지 않음
> **처리 상태(2026-09-20): 완료.** `MemberApiWebMvcTest`에 400 본문(`code`·`fields[0].field`), 404(M001), 403(C003), 405(C004), 500(C001), 409(C007), 400 안전망(C002) 테스트 8건 추가. `MemberApiTest.duplicateEmail`에 `code == M002` 단언 추가. 전부 통과 [측정].
- 증거: `MemberApiTest.kt:81-83` 409 상태만. `MemberApiWebMvcTest.kt:86-92,159-165` 400 상태만. `ApiControllerAdvice.kt:41-47`(fields 매핑), `:51-59`(405), `:61-65`(403), `:67-78`(ConstraintViolation), `:80-84`(500) 실행 경로 없음. `OpenApiConfig.kt:25-28`이 이들을 공개 계약으로 선언.
- 영향: `code`("M002")·`message`·`fields[].field` 형식이 바뀌어도 테스트가 통과한다. 존재하지 않는 회원 활성화 → 404 경로, 405 경로 모두 미검증. OpenAPI 문서와 실제 응답이 어긋나도 감지 불가.
- 수정 방향: `MemberApiWebMvcTest`에 404(`$.code == "M001"`), 400 본문(`$.fields[0].field == "email"`), 405(`C004`) 추가. `MemberApiTest.duplicateEmail`에 `$.code == "M002"`.
- 출처: [측정]

### T-2 [중간] 테스트 더블(Clock, PasswordEncoder)이 `@Primary`/`@Fallback` 없이 "파라미터명 == 빈 이름" 폴백으로 주입됨
> **처리 상태(2026-09-20): 완료.** `SystemClock`, `SecurePasswordEncoder`에 `@Fallback` 부여(DummyEmailSender와 동일 패턴). 전체 테스트 통과 [측정].
- 증거: `adapter/out/SystemClock.kt:7-8`, `adapter/security/SecurePasswordEncoder.kt:7-8` 둘 다 `@Component`만. `SplearnTestConfiguration.kt:26-30` `@Bean fun passwordEncoder()`, `@Bean fun clock()`. `MemberModifyService.kt:26-27` 파라미터명 `passwordEncoder`, `clock`. 대조: `DummyEmailSender.kt:12` `@Fallback` — EmailSender만 올바르게 처리. 저장소 전체에서 `@Fallback`/`@Primary`는 이 한 곳 [측정].
- 영향: 같은 타입 빈이 2개인데 주입이 성공하는 이유는 Spring이 마지막 수단으로 생성자 파라미터명과 빈 이름을 매칭하기 때문이다 [문서지식]. 파라미터명이나 `@Bean` 메서드명이 바뀌면 모든 통합 테스트가 `NoUniqueBeanDefinitionException`으로 일제히 깨진다.
- 수정 방향: `SystemClock`, `SecurePasswordEncoder`에 `@Fallback`(DummyEmailSender와 동일 패턴). 테스트 SKILL §5에 규칙 명시.
- 출처: [측정] / [문서지식] Spring 문서 "qualifier·primary·fallback이 없는 비유일 의존은 주입 지점 이름과 빈 이름을 대조해 같은 이름의 후보를 선택". 검증 패스 `[측정]`: `LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_BEANS_FACTORY_SUPPORT=DEBUG`로 `MemberApiTest` 실행 시 후보 빈 6개가 모두 생성되고 `Autowiring by type from bean name 'memberModifyService' via constructor to bean named 'clock'` / `'passwordEncoder'` 로그 확인. CONFIRMED.

### T-3 [낮음] Instancio 시드가 고정·기록되지 않아 랜덤 실패를 재현할 수 없음
> **처리 상태(2026-09-20): **코드 수정 완료.** `MemberFixture.randomEmail()` 이 `Email` 정규식을 항상 만족하는 형태(`<소문자 16자>@splearn.app`)를 직접 만든다. 시드는 고정하지 않았다 — `instancio.properties` 의 전역 시드는 모든 `create()` 가 같은 값을 내게 해 유니크 키 테스트(`duplicateEmailFail` 등)를 깨뜨린다 [문서지식].**
- 증거: `src/test/resources/instancio.properties:1` `bean.validation.enabled=true`만. `MemberFixture.kt:17-22` `Instancio.gen().net().email()` → `Email(...)` 정규식(`Email.kt:9`, TLD 2~7자) 의존.
- 영향: Kotest에서는 실패 시 시드가 출력되지 않는다 [추정]. 라이브러리 버전 업으로 이메일 생성 규칙이 바뀌면 거의 모든 테스트가 산발적으로 깨지며 재현 불가.
- 수정 방향: `seed` 고정 또는 CI에서만 고정. Fixture에서 도메인 정규식을 만족하는 형식으로 직접 생성.
- 출처: [측정] / [추정]

### T-4 [낮음] QueryService 2종·InstructorRepository 테스트 부재, 유스케이스 실패 경로 미검증
> **처리 상태(2026-09-20): **코드 수정 완료.** `CourseFinderTest`(4), `EnrollmentFinderTest`(4), `InstructorRepositoryTest`(5) 추가. `EnrollerTest` 에 `startStudyingFailNotFound`·`completeFailNotFound`, `CourseCreatorTest` 에 `createFailInstructorNotFound`·`createFailPendingInstructor` 추가 [측정].**
- 증거: `CourseQueryService.kt:12-16`, `EnrollmentQueryService.kt:12-19`, `InstructorRepositoryAdapter.kt:9-22` 대응 테스트 없음. `EnrollerTest.kt` `startStudying`/`complete`에 미존재 id 케이스 없음. `CourseCreatorTest.kt` 미존재·PENDING 강사 케이스 없음.
- 영향: 개발가이드 147행 "usecase/port 패키지를 미러링" 위반. `@Transactional(readOnly)`·`@Validated` 프록시 적용은 여기서만 확인 가능.
- 수정 방향: `CourseFinderTest`, `EnrollmentFinderTest`, `InstructorRepositoryTest` 추가. 실패 케이스 2건 추가.
- 출처: [측정]

### T-5 [낮음] `SplearnApplicationTest`의 static mock이 실패 시 해제되지 않음
> **처리 상태(2026-09-20): **코드 수정 완료.** `afterTest { unmockkStatic(SpringApplication::class) }` 로 실패 시에도 해제된다.**
- 증거: `SplearnApplicationTest.kt:15,22` `mockkStatic` … `unmockkStatic`이 try/finally 없이 직렬.
- 영향: 19행 또는 21행이 실패하면 `SpringApplication.run`이 목 상태로 남아 같은 JVM의 후속 `@SpringBootTest` 컨텍스트 기동에 영향을 준다 [추정].
- 수정 방향: `afterTest { unmockkStatic(...) }` 또는 try/finally.
- 출처: [측정] / [추정]

### T-6 [낮음] 보안 필터 401 경로 미검증, 슬라이스 테스트의 보안 처리 방식 미명시
> **처리 상태(2026-09-20): **코드 수정 완료.** `MemberApiTest.rejectsMalformedBearerToken`(`Bearer not-a-jwt` → 401) 추가 [측정]. 슬라이스가 인증 없이 통과하는 이유를 확정했다 [측정]: Spring Boot 4 는 `spring-boot-security-test` 모듈이 있어야 `@WebMvcTest` 에 보안 자동 구성을 넣는데(`spring-boot-security-4.0.6.jar` 에 `WebMvcTest.imports` 파일 없음) 이 프로젝트는 그 의존성이 없고, `SecurityConfig` 도 슬라이스 포함 타입이 아니다. test SKILL §3-4 에 명시.**
- 증거: `MemberApiWebMvcTest.kt:25-37` 보안 설정·`@WithMockUser` 없음. 슬라이스 6건이 201/200으로 통과 [측정]하나 그 이유는 [미확인]. 통합 테스트에 잘못된 Bearer 토큰 → 401 케이스 없음.
- 수정 방향: `MemberApiTest`에 잘못된 토큰 401 케이스 1건. SKILL에 슬라이스 테스트의 보안 필터 동작 명시.
- 출처: [측정] / [미확인]

### T-7 [낮음] 테스트 규칙 준수 이탈(소소)
> **처리 상태(2026-09-20): **코드 수정 완료.** `MemberRepositoryTest` 가 `BaseRepositoryTest` 를 상속. `MemberRegisterTest` 에서 `MemberLifecycleTest`(activate·deactivate)·`MemberModifierTest`(updateInfo) 분리. `MemberApiWebMvcTest.register` 의 `.exchange()` 제거, `MemberApiTest` 의 `print()`·`SplearnTestConfiguration` 의 `println` 제거.**
- 증거: `MemberRepositoryTest.kt:16-18` `BaseRepositoryTest` 미사용(다른 4개와 불일치). `MemberRegisterTest.kt:51,60,70` `activate`/`deactivate`/`updateInfo`가 `MemberRegisterTest`에 있음(`{TargetClass}Test` 규칙 이탈). `MemberApiWebMvcTest.kt:64-70` `register`만 `.exchange()` 사용. `MemberApiTest.kt:82` `.apply(print())`, `SplearnTestConfiguration.kt:22` `println`.
- 수정 방향: Base 상속 통일, `MemberLifecycleTest`/`MemberModifierTest` 분리, 출력문 제거.
- 출처: [측정]

### T-8 [낮음] 단언 없는 스모크 테스트
> **처리 상태(2026-09-20): **코드 수정 완료.** `DummyEmailSenderTest` 는 `shouldNotThrowAny`, `ProfileTest.profile` 은 `address shouldBe` 단언.**
- 증거: `DummyEmailSenderTest.kt:8-10` 단언 없음. `ProfileTest.kt:9-13` 예외 미발생만 암묵 검증.
- 수정 방향: `shouldNotThrowAny` 래핑 또는 `address shouldBe` 단언.
- 출처: [측정]

### T-9 [낮음] `junit-platform.properties` 설정이 Kotest에서 무효
> **처리 상태(2026-09-20): **완료.** `junit-platform.properties` 삭제. 테스트 209개 통과에 영향 없음 [측정].**
- 증거: `src/test/resources/junit-platform.properties:1` `spring.test.constructor.autowire.mode=all`. JUnit Jupiter `SpringExtension`용 [문서지식]. 어떤 테스트도 생성자 주입을 쓰지 않는다.
- 수정 방향: 삭제.
- 출처: [측정] / [문서지식]

### T-10 [낮음] 리포지토리 테스트는 운영 빈(BCrypt·실시간 Clock)으로 돈다
> **처리 상태(2026-09-20): **코드 수정 완료.** `BaseRepositoryTest` 에 `@Import(SplearnTestConfiguration::class)`. 이제 리포지토리 테스트도 테스트 빈(고정 Clock·단순 인코더)으로 돌고 `@ApplicationServiceTest` 와 같은 컨텍스트 설정을 공유한다. 컨텍스트 캐시 적중 여부는 실측하지 않았다 [추정].**
- 증거: `support/test/BaseRepositoryTest.kt:29-30` `@SpringBootTest @Transactional`만 있고 `@Import(SplearnTestConfiguration)` 없음. 검증 패스 실측 `[측정]`: 이 컨텍스트에서 `memberModifyService → 'dummyEmailSender' / 'securePasswordEncoder' / 'systemClock'` 주입.
- 영향: 리포지토리 테스트가 회원 준비 시 매번 BCrypt(work factor 10)를 수행하고, 시각이 고정되지 않아 시간 비교 단언을 넣을 수 없다. Spring 컨텍스트도 `@ApplicationServiceTest`와 별개로 하나 더 뜬다. 결함은 아니나 의도된 구성인지 불명확.
- 수정 방향: `BaseRepositoryTest`에 `@Import(SplearnTestConfiguration)` 추가 또는 `@ApplicationServiceTest`로 통일.
- 출처: [측정]

### T-11 [낮음] H2 `DEFAULT_NULL_ORDERING=HIGH`는 MySQL의 NULL 정렬과 반대
> **처리 상태(2026-09-20): **완료.** `DEFAULT_NULL_ORDERING=HIGH` 옵션 제거. H2 기본값 `LOW`(NULL 을 낮은 값으로 정렬) 가 MySQL 의 ASC NULL 우선 배치와 같다 [문서지식].**
- 증거: `src/test/resources/application.yml:3`. MySQL은 ASC에서 NULL을 먼저 배치 [문서지식].
- 영향: 현재 ORDER BY 쿼리가 없어 무해. nullable 컬럼(`published_at`) 정렬이 들어오면 테스트와 운영 결과가 갈린다.
- 수정 방향: `LOW`로 변경 또는 옵션 제거.
- 출처: [측정] / [문서지식]

---

## 5. 인프라·운영

### I-1 [중간] CI가 없어 ADR-0008의 PR 규칙을 강제하는 자동 검증이 없음
> **처리 상태(2026-09-20): 파일 추가 완료, 원격 설정 대기.** `.github/workflows/ci.yml`(gitleaks → temurin 25 → `test ktlintCheck detekt` → 리포트 업로드)과 `.github/dependabot.yml`(gradle·actions 주간)을 추가했다. 액션 버전과 temurin 25 가용성은 [미확인]이며 첫 실행에서 확인해야 한다. main 브랜치 보호 규칙에서 이 워크플로우를 필수 체크로 지정하는 것은 GitHub 설정이라 저장소 밖 조치다.
- 증거: `.github/` 없음. `.claude/adr/0008-github-flow-squash-merge.md:21,25` "main 직접 커밋 금지", "PR → Squash Merge". 유일한 자동 검사는 `.claude/settings.json`의 Claude Stop 훅(`ktlintCheck`)으로 로컬 Claude 세션에서만 동작. `build.gradle.kts:11` `detekt { ignoreFailures = true }`.
- 영향: 테스트·아키텍처 테스트·시크릿 스캔이 머지 전에 실행되지 않는다. S-1(키 커밋)이 통과한 것이 그 결과다.
- 수정 방향: GitHub Actions에 `./gradlew test ktlintCheck detekt` + gitleaks + Dependabot을 PR 필수 체크로. main 브랜치 보호 규칙.
- 출처: [측정]

### I-2 [중간] 운영 프로파일이 없고 커넥션 풀·서버 스레드·데이터소스가 모두 기본값에 의존
> **처리 상태(2026-09-20): 완료(초기값).** `application-prod.yml`에 데이터소스 플레이스홀더(기본값 없음, S-9의 H2 폴백도 차단), HikariCP `maximum-pool-size`·`connection-timeout`·`leak-detection-threshold`, `spring.jdbc.template.query-timeout`을 명시했다. 수치는 [추정] 초기값이며 DB 한도와 인스턴스 수를 보고 조정해야 한다. Tomcat 스레드는 기본값을 유지했다.
- 증거: `application.yml:1-26` 전체에 `spring.datasource.*`, `spring.datasource.hikari.*`, `server.tomcat.*`, `spring.jdbc.template.query-timeout` 없음. `application-prod.yml` 등 프로파일 파일 없음.
- 영향: HikariCP 기본 `maximumPoolSize=10`, `connectionTimeout=30s`, Tomcat `threads.max=200` [문서지식]. 요청 200개가 커넥션 10개를 두고 대기하고, P-4·P-5처럼 커넥션을 오래 잡는 코드와 결합하면 30초 대기 후 예외. `leak-detection-threshold` 미설정으로 누수 진단 불가. S-9의 H2 폴백도 이 부재에서 나온다.
- 수정 방향: 운영 프로파일에 풀 크기·타임아웃·leak detection·query timeout 명시. Actuator로 `hikaricp.connections.pending` 노출.
- 출처: [측정] / [문서지식]

### I-3 [낮음] 개발용 compose가 `mysql:latest` + 전 인터페이스 3306 바인딩 + 고정 약한 자격증명
> **처리 상태(2026-09-20): **완료.** `mysql:8.4` 태그 고정, `127.0.0.1:3306:3306`, 비밀번호는 `.env`(gitignore) 에서 읽고 `.env.example` 을 제공. README 에 `cp .env.example .env` 안내. git 이력의 `verysecret` 은 S-1 의 `git filter-repo` 때 함께 지운다(저장소 밖 조치).**
- 증거: `compose.yaml:3,6-7,10`. `git log -S verysecret` → 5개 커밋.
- 영향: `spring-boot-docker-compose`가 `developmentOnly`라 운영 영향은 없다. 개발 머신에서 `3306:3306`은 `0.0.0.0` 바인딩이라 같은 네트워크에서 root 접속 가능. `latest` 태그는 재현성 위험(V2의 `DROP CONSTRAINT` 문법은 8.0.19+ 의존).
- 수정 방향: `127.0.0.1:3306:3306`, 이미지 태그 고정, 비밀번호는 `.env`(gitignore).
- 출처: [측정]

### I-4 [낮음] detekt 리포트가 잡음으로 덮여 실제 발견을 가림
> **처리 상태(2026-09-20): **완료.** `config/detekt/detekt.yml` 에 `MagicNumber.ignoreEnums: true`(23건 소거), `config/detekt/baseline.xml` 에 남은 2건(`Curriculum` TooManyFunctions 14>11, `ApiControllerAdvice` 의 `>= 500` MagicNumber — S-2 처리 때 들어온 것) 고정 후 `ignoreFailures = false`. `./gradlew detekt` 발견 0건·BUILD SUCCESSFUL [측정]. 이제 baseline 밖 새 발견은 로컬·CI 빌드를 깬다. CLAUDE.md·개발가이드·test SKILL 의 "리포트를 직접 읽는다" 문구를 갱신했다.**
- 증거: `[측정]` 24건 중 23건이 `ErrorCode.kt:8-29`의 HTTP 상태 코드 리터럴 MagicNumber. `ignoreFailures = true`라 아무도 읽지 않는다. 나머지 1건은 WIP `Curriculum` TooManyFunctions(14 > 11).
- 영향: 새 발견이 추가돼도 23건 잡음 속에 묻힌다.
- 수정 방향: `detekt.yml`에 `MagicNumber.ignoreEnums: true` 또는 `ErrorCode`를 `HttpStatus` 상수 참조로. baseline 파일 도입 후 `ignoreFailures = false`.
- 출처: [측정]

---

## 6. 문서-코드 괴리

| 위치 | 문제 | 출처 |
|---|---|---|
| `README.md:53` | "현재 구현된 도메인은 회원(Member) 애그리거트입니다." Course·Curriculum·Enrollment·Instructor가 도메인·애플리케이션 계층에 구현되어 있다. | [측정] |
| `README.md:149-155` | DB 마이그레이션 표에 V5(instructor)·V6(course)·V7(enrollment)가 없고 V4에서 V8로 건너뛴다. | [측정] |
| `README.md` 용어 사전 ↔ `용어사전.md` | 거의 동일한 내용을 두 곳에 보유. | [측정] |
| `.claude/skills/test/SKILL.md:213-216` | 테스트 구성을 "EmailSender no-op, PasswordEncoder" 두 빈으로 기술하나 실제 `SplearnTestConfiguration.kt:22`는 `println`, `:29-30`에 Clock 빈(고정 시각)이 추가됨. HexagonalArchitectureTest를 Konsist 단일 테스트로 예시하나 실제 5개. | [측정] |
| `개발가이드.md` | 코드와 거의 완벽히 일치. 특히 67-69행의 CurriculumCreator/Validator DIP 역전 구조 서술이 실제 코드와 정확히 일치. | [측정] |
| `ADR-0009`, `ADR-0010` | 위반 없음. V1~V7 미수정, GET 엔드포인트 없음, DummyEmailSender 로그만. | [측정] |

---

## 7. WIP 미완성 (결함 아님)

- Course/Curriculum/Enrollment/Instructor의 `adapter.webapi`(REST 컨트롤러)가 없다. `MemberApi`만 존재. 유스케이스·리포지토리·도메인은 완성도가 높고 통합 테스트도 갖춰져 있다.
- Curriculum 테스트 4개(도메인 16건, 리포지토리 8건, 코디네이터 20건, 파인더 11건)는 전부 통과. 개발가이드 133행이 약속한 "자식 id 유지"는 `CurriculumRepositoryTest.keepsSectionAndLessonIdsAcrossResave`가 검증한다.
- 남은 보강 후보: `Section`·`Lesson` 직접 단위 테스트, `CurriculumCoordinator.validate`의 미존재 courseId 케이스, 섹션·수업 제목 길이 검증(S-3 참고).
- `Curriculum` 함수 14개(detekt TooManyFunctions). 편집 API가 많은 애그리거트의 자연스러운 크기인지, 섹션 조작을 `Section`으로 내릴지는 설계 판단.

---

## 8. 확인했으나 이상 없음

보안
- JWT 알고리즘·키 길이: HS256, 32바이트(256bit) 키로 최소 요건 충족.
- BCrypt 강도: `$2a$10$` (work factor 10).
- SQL 인젝션: `@Query`는 명명 파라미터 바인딩, 나머지는 파생 쿼리, 문자열 연결 없음. LIKE 와일드카드는 Spring Data Relational `Escaper`가 처리.
- 대량 할당: Command는 명시 필드만, 도메인은 팩토리로만 생성. `id`/`status`/`passwordHash` 바인딩 경로 없음.
- 에러 응답: 500은 고정 메시지만, 스택·SQL·클래스명 미노출(실측).
- 로그인 실패 원인 비구분(`LoginFailedException` 단일). 계정 열거 방지 의도 명확.
- CSRF 비활성: STATELESS + Bearer 구조라 타당. 쿠키 인증 도입 시 재검토.
- 액추에이터: 기본 노출(health만). `/actuator/prometheus`·`/env` 404 실측. H2 콘솔 404.
- DB 자격증명 커밋 없음. 응답 DTO에 `passwordHash` 없음. 테스트용 취약 인코더는 `src/test`에만.
- 잘못된 Bearer 토큰은 permitAll이어도 401로 거부(실측).

성능
- `findByEmailAddress`, `findByProfileAddress`, `findByMemberId`(instructor), `findByInstructorId`, `findByInstructorIdAndTitle`, `findByMemberIdAndCourseId`, `findByCourseId`(curriculum) 모두 인덱스 또는 유니크 제약 적중.
- `SpringMemberRepository.kt:7-25` 수동 `@Query` LEFT JOIN으로 member+detail 단일 쿼리 로딩. 좋은 선택.
- 다섯 가지 check-then-act 모두 DB 유니크 제약으로 중복 행 차단.
- 같은 애그리거트 중복 로딩 없음. `List` + `keyColumn` 선택은 위치 인덱스 기반 편집 도메인에 타당.
- `@ApplicationService`/`@QueryApplicationService`에 `@Transactional`/`readOnly` 분리 존재. 모든 Query 서비스가 readOnly 스테레오타입 사용.

설계
- 도메인 계층에 Spring 애너테이션 0개. `LocalDateTime.now()` 직접 호출 0건(전부 `Clock` 포트 경유).
- 애그리거트 간 참조는 전부 ID. 팩토리 메서드에서만 전체 객체를 받아 불변식 확인.
- `application.curriculum` → `application.course.port` 단방향 DIP 역전. 순환 없음.
- 예외 계층(`SplearnException` + `ErrorCode`) 5개 애그리거트 일관.
- `*RepositoryAdapter`의 `getById = findById ?: throw` 반복은 아키텍처 SKILL §7의 baseline 패턴과 일치. 예외 타입이 다르므로 추출 대상 아님.
- ADR-0009 위반 없음(V8 신규 추가만).

테스트
- Mockito·`@MockBean`·JUnit `@Test`·`relaxed`·`spyk` 사용 0건. Kotest FunSpec + MockK 일관.
- `every`/`verify` 쌍 누락 없음. WebMvc 실패 케이스는 strict `mockk()`라 유스케이스 호출 시 실패로 암묵 검증.
- Flyway V1~V8 전부 H2 MySQL 모드에서 실행 성공. `ENGINE`/`CHARSET` 절 없음.
- `ArchUnitSliceLearningTest`가 `Target.activate()` 위반을 실제로 잡음. 슬라이스 조건 실효성 증명.
- `SplearnTestConfiguration`의 Clock 고정으로 시간 의존 비결정성 없음.

---

## 9. 검증 패스 결과

리뷰어 4명과 별개의 검증자가 심각도 높음 항목과 `[추정]` 항목 7개를 재검증했다. 저장소 파일은 건드리지 않았고 `./gradlew test --tests ... --rerun` 3회만 실행했다(전부 통과). 아래 세 건은 원 주장을 수정·기각하거나 확대했다.

| 주장 | 판정 | 결과 반영 |
|---|---|---|
| Curriculum 저장 시 자식 전체 삭제·재삽입 | CONFIRMED [측정] | P-2에 문장 6개 표 추가. id 보존 확인으로 정확성 우려 제거 |
| 검증 실패 로그에 rejected value 노출 | PARTIAL [측정] | S-2. 포맷은 email·nickname 필드로 재확인, 비밀번호 필드는 보안 리뷰어의 jar 실측에 의존 |
| Clock·PasswordEncoder 주입이 파라미터명 폴백에 의존 | CONFIRMED [측정] | T-2에 Spring DEBUG 로그 근거 추가 |
| Konsist가 같은 레이어 내부 의존을 검사하지 않음 | CONFIRMED [측정: 소스] | **확대.** 비-strict `dependsOn`은 위반 자체를 만들지 않아 강제 규칙이 `dependsOnNothing` 하나뿐. M-1을 중간 → 높음으로 |
| H2/MySQL 대소문자 비교 차이 | CONFIRMED(분석적) | S-11. 단 "운영에서 409 대신 500"이라는 두 리뷰어의 결론은 **기각**. MySQL은 사전 조회도 같은 콜레이션으로 비교해 409가 나온다. 실제 문제는 테스트 보증 불가와 정책 부재 |
| BCrypt·EmailSender가 트랜잭션 안 | CONFIRMED [측정] | P-4, P-5 |
| 운영 jar의 H2 폴백 | CONFIRMED(정적) | S-9 |

검증자의 추가 발견: `BaseRepositoryTest` 컨텍스트는 운영 빈(BCrypt·SystemClock·DummyEmailSender)으로 돈다 → T-10 신설. 재저장 시 루트 `UPDATE curriculum`이 변경 유무와 무관하게 항상 실행된다 → P-2에 반영.

리뷰 방법의 한계:
- 보안 리뷰어가 `bootJar` + 로컬 기동으로 얻은 실측(S-2, S-3, S-4, S-5, S-6, S-7, S-9)은 검증자가 재실행하지 않았다. 코드 경로는 검증자가 정적으로 재확인했다.
- MySQL 실제 동작(콜레이션, FK 자동 인덱스, DDL 문법)은 모두 [문서지식]이다. 이 프로젝트에 MySQL 통합 테스트가 없기 때문이며, 그 자체가 S-11·T-11의 지적 사항이다.
- 성능 항목은 전부 정적 분석이다. 수치 임계값은 제시하지 않았다.

---

## 10. 범위 밖·보류

- 발견사항 수정. 항목별로 별도 요청.
- 의존성 버전 안정성: `detekt 2.0.0-alpha.3`, `instancio 6.0.0-RC2`, `mysql:latest`. 존재만 기록.
- 인가 모델 설계(본인 확인 방식, 역할 구분). S-6 결정 시 함께.
- 시크릿 관리 방식(환경변수 vs 볼트). S-1 수정 단계에서.
- 성능 목표 수치. 부하 프로파일이 생기면.
- 이 보고서의 커밋 여부와 반복 리뷰 파일명 규칙.
