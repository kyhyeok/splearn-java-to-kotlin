# Splearn 개발가이드

## 기술 스택
- Kotlin 2.3, JDK 25, Spring Boot 4.0.x
- Spring Web, Spring Data JDBC, Flyway, Bean Validation
- Spring Security OAuth2 Resource Server(JWT HS256), BCrypt
- springdoc-openapi(Swagger UI), Actuator + Prometheus, kotlin-logging
- DB: MySQL(운영, Docker Compose), H2 MySQL 모드(테스트)
- 테스트: Kotest 6(FunSpec), MockK, Konsist
- 코드 품질: ktlint, detekt

## 아키텍처
- 헥사고날 아키텍처 + 도메인 모델 패턴
- 의존성 방향: 외부(Actor) → 어댑터 → 애플리케이션 → 도메인 (안쪽으로만)
- Konsist `HexagonalArchitectureTest`로 계층 의존성 강제. 위반 시 테스트 실패

### 계층
- **Domain Layer**: 핵심 비즈니스 규칙. 다른 계층/프레임워크에 의존 안 함(`dependsOnNothing`)
- **Application Layer**: 유스케이스 조율. 포트(인터페이스)를 정의하고 구현
- **Adapter Layer**: 외부 연동(웹, DB, 메일, 보안, 시간). 포트 구현 또는 호출

## 패키지
```
kimspring.splearn
├── domain
│   ├── member            // 회원 애그리거트 (Member, MemberDetail, Profile, 도메인 예외 ...)
│   └── shared            // 공유 타입 (Email, Clock, SplearnException, ErrorCode)
├── application
│   └── member
│       ├── usecase       // 기능 제공 포트 (어댑터가 호출하는 유스케이스 인터페이스)
│       ├── port          // 기능 요구 포트 (서비스가 필요로 하는 인터페이스)
│       └── command       // 유스케이스 입력 객체 (Bean Validation 제약 포함)
└── adapter
    ├── webapi            // REST 컨트롤러(+ dto), OpenAPI 설정
    ├── out
    │   ├── persistence   // Spring Data JDBC 구현
    │   └── SystemClock   // Clock 포트 구현
    ├── integration       // 외부 시스템 연동 (메일 등)
    ├── security          // 인증/암호화 (PasswordEncoder 구현, SecurityConfig)
    └── ApiControllerAdvice  // 전역 예외 처리
```
- 각 도메인/애플리케이션은 `domain/<name>`, `application/<name>/{usecase,port,command}` 로 동일 구조 반복

## 애그리거트와 애플리케이션 컴포넌트 의존관계
- `domain` 패키지 아래 첫 번째 패키지는 각각 하나의 애그리거트이다 (`shared` 는 예외 — 공유 타입 전용)
- `application` 패키지 아래 첫 번째 패키지는 같은 이름의 애그리거트에 대한 애플리케이션 서비스 패키지이다
- 위에서 구분한 패키지를 **슬라이스(slice)** 라고 부른다
- 하나의 도메인과 애플리케이션 서비스 슬라이스를 묶어 **애플리케이션 컴포넌트** 라고 한다
- 애그리거트는 다른 애그리거트 루트를 직접 참조할 수 있다. 조회 메서드와 값 반환 메서드 사용 가능.
  단, 다른 애그리거트를 변경하는 메서드는 사용할 수 없다
- 애플리케이션 서비스는 다른 애플리케이션 서비스의 `usecase` 포트 인터페이스와 `command` 객체를 사용할 수 있다.
  단, 구현 클래스는 직접 액세스하지 않는다
- 도메인과 애플리케이션 서비스의 슬라이스는 순환 의존성을 허용하지 않는다. 단방향 의존만 가능하다

## 포트 패턴
- **usecase**: 기능 제공 포트 인터페이스. 하나의 책임 단위로 잘게 나눈다(ISP)
    - 예) `MemberRegister`, `MemberLifecycle`, `MemberModifier`, `MemberFinder`, `MemberAuthenticator`
    - 구현체는 `application.member` 의 `~Service` 클래스. 한 서비스가 여러 유스케이스를 구현할 수 있다
      (`MemberModifyService` → `MemberRegister`, `MemberLifecycle`, `MemberModifier`)
    - 컨트롤러는 구현체가 아닌 유스케이스 인터페이스에 의존한다
- **port**: 서비스가 의존하는 기능 요구 포트 인터페이스. 예) `MemberRepository`, `EmailSender`
    - 구현은 adapter 계층(`MemberRepositoryAdapter`, `DummyEmailSender`)이 담당
    - 기본/폴백 구현체는 `@Fallback @Component` 로 등록해 실제 구현이 추가되면 자동으로 밀려나게 한다
- 도메인이 필요로 하는 기술 추상화는 도메인에 인터페이스, adapter 에 구현
    - `domain.member.PasswordEncoder` → `adapter.security.SecurePasswordEncoder`(BCrypt)
    - `domain.shared.Clock` → `adapter.out.SystemClock`. 도메인은 `LocalDateTime.now()` 를 직접 호출하지 않고
      현재 시각을 파라미터로 받는다
- **`find` / `get` 구분**: `find...` 는 `T?` 반환(없으면 null), `get...` 은 `T` 반환(없으면 `~NotFoundException`)

## 도메인 모델 관례
- 엔티티·값 객체 모두 불변 `data class`. 모든 프로퍼티는 `val`, `var` 노출 금지
- 상태 변경은 의미 있는 도메인 메서드가 `copy()` 로 새 인스턴스를 반환한다(`activate()`, `deactivate()`, `updateInfo()`)
- 식별자는 `val id: Long? = null` — 저장 전에는 null, 저장 후 어댑터가 채운다
- 생성은 `companion object` 팩터리 메서드로 의미를 부여: `Member.register(...)`, `MemberDetail.create(...)`
- 불변식 검증
    - 값 객체는 `init` 블록에서 `require(...)`(예: `Email`, `Profile`)
    - 상태 전이 전제 조건은 `check(...)` 또는 도메인 예외 throw(예: `Member.activate()` → `InvalidMemberStateException`)
- 도메인 예외는 `SplearnException` 을 상속하고 `ErrorCode` 를 보유한다. HTTP 상태·에러 코드는 `ErrorCode` 한곳에서 관리
- 도메인 클래스에 Spring/영속성 애노테이션을 붙이지 않는다. `@Table`, `@Id` 는 `~JdbcEntity` 에만
- null 안전성은 Kotlin 타입 시스템으로 처리. 컴파일 옵션 `-Xjsr305=strict`

## 애플리케이션 서비스 관례
- 표준 애노테이션: `@Service @Transactional @Validated` (조회 전용은 `@Transactional(readOnly = true)`)
- 의존성은 생성자 파라미터 `private val` 로 주입. `@Autowired` 필드 주입·`lateinit` 금지
- 입력 검증은 `usecase` 포트 메서드 파라미터에 `@Valid` 부착, `command` 객체에 `@field:` 제약 부착
- 도메인 규칙은 도메인에, 조율·중복 검사·트랜잭션 경계는 서비스에
- 책임별 서비스 분리
    - `MemberModifyService` — 등록·상태 전이·정보 수정 (`@Transactional`)
    - `MemberQueryService` — 조회 (`readOnly`)
    - `MemberAuthenticationService` — 로그인 인증 (`readOnly`)
- 로깅은 kotlin-logging: 파일 상단에 `private val log = KotlinLogging.logger {}`

## 어댑터 관례
- 웹: `@RestController` + 생성자 주입, 유스케이스 인터페이스만 주입. Repository·SDK 직접 주입 금지
- OpenAPI 명세는 `~ApiSpec` 인터페이스에 분리한다. `@RequestMapping`, `@Operation`, `@ApiResponse`,
  `@Valid` 는 스펙 인터페이스에, 구현은 `~Api` 클래스에(SRP)
- 공통 응답 코드(400/403/405/500)는 `OpenApiConfig.globalResponseCustomizer` 로 전역 등록
- 응답 DTO는 `data class` + `companion object { fun of(domain) }` 변환
- 예외 처리는 `@ControllerAdvice`(`ApiControllerAdvice`)에서 `ErrorResponse`(code·message·fields)로 통일.
  `ResponseEntityExceptionHandler` 를 상속해 Spring MVC 기본 예외도 같은 포맷으로 변환한다
- 영속성: `~JdbcEntity`(DB 표현) ↔ 도메인 변환을 `toDomain()` / `from(domain)` 으로 양방향 제공
    - `MemberRepositoryAdapter` 가 `MemberRepository` 포트를 구현하고 Spring Data 인터페이스
      (`SpringMemberRepository : CrudRepository`)를 감싼다
    - Spring Data 인터페이스는 `adapter.out.persistence` 밖으로 노출하지 않는다
    - JPA 미사용 — `@Entity`, `@ManyToOne`, `ddl-auto` 없음. 조인 조회는 `@Query` 로 명시
- 스키마는 Flyway 로만 변경한다. **이미 적용된 `V*` 파일은 수정·삭제 금지**, 변경은 새 `V{N+1}__*.sql` 로
- 보안: `SecurityConfig` 는 stateless + JWT 리소스 서버. 비밀번호는 BCrypt

## 데이터 전달 객체
- 웹 요청은 `application.member.command` 의 커맨드 객체를 `@RequestBody` 로 직접 받는다. 별도 웹 요청 DTO를 두지 않는다
- 커맨드는 `toInfo()` 로 도메인 입력 객체(`MemberRegisterInfo`)로 변환한 뒤 도메인에 전달한다
- 도메인은 웹 DTO를 직접 받지 않는다
- 응답 DTO는 `adapter.webapi.dto` 에 두고 도메인 객체를 그대로 노출하지 않는다

## 테스트 관례
- Kotest `FunSpec` 사용. JUnit5 `@Test` 방식 금지, 모킹은 MockK 전용(Mockito 금지)
- 테스트 데이터는 `MemberFixture` 등 `object` 픽스처의 팩터리 메서드로 생성
- 유스케이스 테스트는 `usecase`/`port` 패키지를 미러링하여 작성
- Spring 컨텍스트가 필요한 테스트는 `extension(SpringExtension())` + `@Import(SplearnTestConfiguration::class)`
    - `SplearnTestConfiguration` 이 `EmailSender`, `PasswordEncoder`, `Clock`(고정 시각) 을 테스트용으로 교체한다
- 웹 계층 단위 테스트는 `@WebMvcTest` + `MockMvcTester`, 유스케이스는 `@TestConfiguration` 에서 `mockk()` 로 주입
- 통합 테스트는 `@SpringBootTest @AutoConfigureMockMvc @Transactional`
- 아키텍처 규칙 위반은 Konsist `HexagonalArchitectureTest` 로 자동 검증
- 테스트는 H2 MySQL 모드에서 운영과 동일한 Flyway 마이그레이션을 실행한다

## 코드 품질
- `./gradlew ktlintCheck` — 포맷 위반 시 빌드 실패. `ktlintFormat` 으로 자동 수정
- `./gradlew detekt` — `ignoreFailures = true` 라 빌드를 막지 않으므로 리포트를 직접 확인한다

## 네이밍
- 기능 제공 포트: 동작 주체 명사(`MemberRegister`, `MemberFinder`, `MemberLifecycle`, `MemberModifier`)
- 서비스 구현: `<도메인><책임>Service`. `~ServiceImpl` 금지
- 기능 요구 포트: `<도메인>Repository`, `<기능>Sender`
- REST 컨트롤러: `<도메인>Api`, OpenAPI 명세 인터페이스: `<도메인>ApiSpec`
- 영속성 엔티티: `<도메인>JdbcEntity`, 포트 구현 어댑터: `<포트>Adapter`
- 커맨드: `<동작><도메인>Command`(`RegisterMemberCommand`, `LoginMemberCommand`)
- 예외: `<상황>Exception`(`DuplicateEmailException`, `LoginFailedException`)
