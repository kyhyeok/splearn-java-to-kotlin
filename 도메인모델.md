# Splearn 도메인 모델

## 도메인 모델 만들기
1. 듣고 배우기
2. '중요한 것'들 찾기 (개념 식별)
3. '연결 고리' 찾기 (관계 정의)
4. '것'들을 설명하기 (속성 및 기본 행위 명시)
5. 그려보기 (시각화)
6. 이야기 하고 다듬기 (반복)

## Splearn 도메인
- 스프런은 회원이 강의를 수강하는 온라인 서비스이다.
- 스프런은 스프링 프레임워크의 철학을 바탕으로 개발되고 운영되며 발전하는 학습 생태계를 목표로 한다.  
  이 생태계를 구성하는 핵심 구성 요소는 학습(성장)하는 회원이다.
    - 그래서 이름이 Spring + Learner = Splearn
    - 유사한 이름으로 인프런이 있다.
- 스프런에서 활동하기 위해서는 회원으로 등록해야 한다.
    - 다만, 회원이 되기 전에도 스프런에 대한 소개와 강의 정보를 살펴볼 수 있다.
    - 강의를 수강하기 위해서는 등록을 완료하고 활동 가능한 회원이 되어야 한다.
    - 등록 신청을 한 뒤 정해진 요건을 충족하면 등록이 완료된다.
    - 등록을 완료한 경우에 프로필 주소, 자기 소개를 등록하거나 수정할 수 있다.
        - 프로필 주소는 알파벳과 숫자로 구성된 15자리 이내의 중복되지 않은 값
        - 탈퇴한 회원의 프로필 주소와 자기 소개는 수정할 수 없다.
    - 등록 시간, 등록 완료 시간, 탈퇴 시간을 저장한다.
- 원하는 회원은 자신이 가진 지식과 경험을 강의라는 형태로 다른 회원에게 제공하는 강사가 될 수도 있다.
    - 회원이 최초로 강사가 되려면 강사 신청과 승인 과정을 거치도록 한다.
    - 승인된 강사는 강의를 생성하고 검수를 거쳐 강의를 공개할 수 있다.
    - 공개된 강의는 회원에게 노출되고, 회원은 이 강의를 수강할 수 있다.
- 수강은 강의를 학습하는 것을 말한다. "내가 수강한 강의는 a이다. 나는 b 강의 수강중이다."
    - 수강을 위해서는 먼저 수강 신청이 필요하다.
    - 수강 신청은 강의의 수강 요건에 따라서 신청을 해야 하고 이후 추가 절차가 요구될 수 있다.
    - 일부 강의는 수강 신청과 동시에 수강이 가능하다.
    - 어떤 강의는 수강 신청후 강의비 결제를 완료해야 수강이 가능하다.
    - 강사는 강의 수강 기간의 제한을 둘 수도 있다.
- 강의는 영상, 문서와 같은 컨텐츠를 가진 하나 이상의 수업으로 구성 된다.
    - 수업은 갯수가 많을 수 있다. 그래서 수업은 다시 섹션으로 구분한다.
    - 하나의 강의는 여러 개의 섹션과 각 섹션에 속한 수업으로 구성된다.
    - 수업과 섹션은 학습 순서를 가진다.
    - 학습 진도는 매 수업 단위로 기록된다.
    - 강의의 모든 수업의 학습을 마치고 수업 진도가 100%에 도달하면 강의 수강을 완료한 것이다.

## 도메인 모델

---
### [회원 애그리거트]
`domain.member`

### 회원(Member)
_Aggregate Root_
#### 속성
- `id`: `Long?` 저장 전에는 `null`
- `email`: `Email` 이메일 - Natural ID
- `nickname`: 닉네임
- `passwordHash`: 비밀번호 해시
- `status`: `MemberStatus` 회원 상태
- `detail`: `MemberDetail` 1:1
#### 행위
- `register(MemberRegisterInfo, PasswordEncoder, LocalDateTime)`: 회원 등록. `companion object`
- `activate(LocalDateTime)`: 등록을 완료시킨다
- `deactivate(LocalDateTime)`: 탈퇴시킨다
- `verifyPassword(String, PasswordEncoder)`: 비밀번호를 검증한다
- `changePassword(String, PasswordEncoder)`: 비밀번호를 변경한다
- `updateInfo(nickname, profileAddress, introduction)`: 닉네임, 프로필 주소, 자기 소개를 수정한다
- `isActive()`: 등록 완료 상태인지 확인한다
#### 규칙
- 불변 `data class`. 모든 행위는 상태를 바꾸지 않고 `copy()`로 새 `Member`를 반환한다
- 회원 생성후 상태는 등록 대기
- 등록 대기 상태에서만 등록 완료가 될 수 있다
- 등록 완료 상태에서만 탈퇴할 수 있다
- 등록 완료 상태에서만 회원 정보를 수정할 수 있다
- 상태 규칙을 위반하면 `InvalidMemberStateException`
- 회원의 비밀번호는 해시를 만들어서 저장한다
- 현재 시각은 인자로 받는다. 도메인은 시간에 직접 의존하지 않는다
- 상세 정보 변경은 `MemberDetail`에 위임한다
- 프로필 주소는 중복을 허용하지 않는다. 중복 검사는 애플리케이션 계층(`MemberModifyService`)에서 수행한다

### 회원 상세(MemberDetail)
_Entity_
#### 속성
- `profile`: `Profile?` 프로필 주소
- `introduction`: 자기 소개
- `registeredAt`: 등록 일시
- `activatedAt`: 등록 완료 일시
- `deactivatedAt`: 탈퇴 일시
#### 행위
- `create(LocalDateTime)`: 전달받은 시간을 등록 일시로 저장한다. `companion object`
- `recordActivation(LocalDateTime)`: 등록 완료 일시를 저장한다
- `recordDeactivation(LocalDateTime)`: 탈퇴 일시를 저장한다
- `updateInfo(profileAddress, introduction)`: 프로필 주소, 자기 소개를 수정한다
#### 규칙
- 불변 `data class`. Kotlin에는 package-private이 없어 가시성 대신 불변성으로 보호하고, 변경은 `Member`를 거친다
- 등록 완료 일시, 탈퇴 일시는 한 번만 저장할 수 있다
- 프로필 주소가 빈 값이면 `null`로 저장한다 (프로필 주소 제거)

### 회원 상태(MemberStatus)
_Enum_
#### 상수
- `PENDING`: 등록 대기
- `ACTIVE`: 등록 완료
- `DEACTIVATED`: 탈퇴

### 회원 등록 정보(MemberRegisterInfo)
_Value Object_
#### 속성
- `email`: 이메일
- `nickname`: 닉네임
- `password`: 비밀번호
#### 규칙
- 형식·길이 검증은 애플리케이션 계층의 `RegisterMemberCommand`(`@Email`, `@Size`)가 담당한다
- 회원 정보 수정 입력도 같은 이유로 애플리케이션 계층의 `UpdateMemberInfoCommand`에 둔다

### DuplicateEmailException
_Exception_
- 이미 사용중인 이메일로 등록하면 발생. `M002` / 409

### DuplicateProfileException
_Exception_
- 이미 존재하는 프로필 주소로 수정하면 발생. `M003` / 409

### InvalidMemberStateException
_Exception_
- 허용되지 않는 상태에서 등록 완료·탈퇴·정보 수정을 시도하면 발생. `M004` / 400

### MemberNotFoundException
_Exception_
- 존재하지 않는 회원 id로 조회하면 발생. `M001` / 404

### 비밀번호 인코더(PasswordEncoder)
_Domain Service_
#### 행위
- `encode()`: 비밀번호 암호화하기
- `matches()`: 비밀번호가 일치하는지 확인

### 프로필 주소(Profile)
_Value Object_
#### 속성
- `address`: 프로필 주소
#### 행위
- `url()`: `@주소` 형태로 반환한다
#### 규칙
- 소문자 알파벳과 숫자로 구성된 15자 이내
- 빈 문자열은 허용하지 않는다. 프로필 주소 없음은 `MemberDetail.profile == null`로 표현한다

---
### [공용]
`domain.shared`

### Email
_Value Object_
#### 속성
- `address`: 이메일 주소
#### 규칙
- 이메일 형식이어야 한다

### Clock
_Domain Service_
#### 행위
- `now()`: 현재 시각을 반환한다
#### 규칙
- 도메인 행위에 필요한 현재 시각은 애플리케이션 계층이 주입한다

### 에러 코드(ErrorCode)
_Enum_
#### 속성
- `status`: HTTP 상태 코드
- `code`: 에러 코드. 공통은 `C###`, 회원은 `M###`
- `message`: 기본 메시지

### SplearnException
_Exception_
- 모든 도메인 예외의 상위 타입. `ErrorCode`를 가진다

---


### [미구현]

### 강사

### 강의

### 수업

### 섹션

### 수강

### 진도